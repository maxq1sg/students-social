import React from "react";
import Loader from "../../Loader/Loader";
import StyledButton from "../../StyledButton/StyledButton";

const MainPage = () => {
  return <StyledButton disabled={false}>отправить</StyledButton>;
};

export default MainPage;
